package Java.이전;

import java.util.Arrays;
import java.util.List;

public class test735 {
 /*   public static  void main(String[] args) {
        int[] test = {5,10,-5};
        asteroidCollision(test);
    }
    public static int[] asteroidCollision(int[] asteroids) {
       int i = 0;
       int j = 1;
       List<int[]> intlist = Arrays.asList(asteroids);
       System.out.println(intlist.get(i));

       while(){
           //앞에 숫자는 0보다 크고 다음 오는 숫자가 0보다 작을 때
           if(intlist.get(i).){
                //숫자 크기 비교해서 작은거 pop
               if(asteroids[i] > asteroids[j]){
                   asteroids[i]
               }else if(asteroids[i] < asteroids[j]){

               }else{

               }
                i++;
                j++;
                System.out.println();
            }
       }
        return null;
    }*/

}
